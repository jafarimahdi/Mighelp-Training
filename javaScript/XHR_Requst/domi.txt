Hi, we try to practice on XML HTTP Requests fom the bigging again 
add the normal text is easy to get but we try to make event and use for it .
you can add data in the TXT file and view it in the browser.